package:
  name: pymt
  version: "0.1.1"

source:
  path: ..

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy >=0.17.0
    - nose
    - shapely
    - netcdf4
    - pyyaml
    - esmpy
    - cfunits-python

  run:
    - python
    - setuptools
    - numpy
    - scipy >=0.17.0
    - nose
    - shapely
    - netcdf4
    - pyyaml
    - esmpy
    - cfunits-python

about:
  summary: The CSDMS Python Modeling Tools

build:
  number: 2
  string: {{ environ.get('BUILD_STR', '') }}
